# Plain HTTP — what the deployment has always been, and still the fallback
# when no certificate is present. See nginx/tls.conf for the other half.
server {
    listen 80;
    server_name _;

    include /etc/nginx/settled/app.conf;
}
